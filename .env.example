# Polymarket Trading Bot Configuration
# Copy this to .env and fill in your values

# ==============================================================================
# REQUIRED: Wallet Configuration
# ==============================================================================
# For proxy wallets (Magic/email), these are DIFFERENT:
# - PRIVATE_KEY: Your signing key (for authentication)
# - FUNDER: The address that actually holds your funds
# For EOA wallets (MetaMask), these are the SAME address

POLYMARKET_PRIVATE_KEY=0x...
POLYMARKET_FUNDER=0x...  # The address holding your funds

# ==============================================================================
# API Credentials (Auto-generated on first run)
# ==============================================================================
# Leave these empty initially - they'll be generated from your private key
# on the first run. Copy the generated values here after the first run.
#
# Note: These don't expire, but only one set can be active per wallet.
POLYMARKET_API_KEY=
POLYMARKET_API_SECRET=
POLYMARKET_API_PASSPHRASE=

# ==============================================================================
# Trading Configuration (Optional - defaults shown)
# ==============================================================================
INITIAL_BALANCE=1000.0
MIN_PROFIT_THRESHOLD=0.01
MIN_CONFIDENCE=0.8
PAPER_TRADING=true
DEVELOPMENT=true
